$schema: https://azuremlschemas.azureedge.net/latest/amlCompute.schema.json
name: reservaiq-cpu
type: amlcompute
size: Standard_DS2_v2
min_instances: 0
max_instances: 1
idle_time_before_scale_down: 120
tier: dedicated
description: CPU académica de ReservaIQ. Validar cuota, región y presupuesto antes de crear.
